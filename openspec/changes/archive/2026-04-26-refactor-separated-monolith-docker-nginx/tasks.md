## 1. 仓库结构与契约基线

- [x] 1.1 盘点现有目录并落地前后端分离后的目标结构（如 `frontend/`、`backend/`、`deploy/nginx/`），补充迁移说明文档
- [x] 1.2 统一后端 API 路径前缀与版本策略（如 `/api/v1`），并整理前端调用入口配置
- [x] 1.3 建立前后端契约校验机制（最小可行可为接口 schema 校验或契约测试脚本）
- [x] 1.4 在 CI 中拆分前端与后端独立构建/测试任务，输出分别可见的结果报告

## 2. Docker 化与运行一致性

- [x] 2.1 为前端、后端分别编写生产向 Dockerfile（优先多阶段构建）
- [x] 2.2 编写 `docker-compose.yml` 编排 `frontend`、`backend`、`nginx` 三服务及共享网络
- [x] 2.3 补齐关键环境变量模板（如 `.env.example`）并在缺失时显式失败
- [x] 2.4 为后端与网关配置健康检查并验证 `docker compose up -d` 一键可用

## 3. Nginx 域名与反向代理

- [x] 3.1 新增 Nginx 站点配置，绑定 `server_name shortlink.nym.asia` 并托管前端静态资源
- [x] 3.2 配置 `/api` 反向代理到后端服务，透传必要请求头并保留响应状态码
- [x] 3.3 配置 SPA 路由回退规则，验证深链访问与刷新行为正确
- [x] 3.4 编写并执行网关冒烟用例（首页访问、API 转发、后端异常场景）

## 4. 去营销化内容治理

- [x] 4.1 在前端页面、公共组件与文档中清理“拿个Offer”“马丁”等营销化关键词
- [x] 4.2 统一项目文案为中性工程表述（个人实习项目定位、技术目标、可验证成果）
- [x] 4.3 增加可复检的关键词扫描脚本并纳入提交前或 CI 检查
- [x] 4.4 对首页、README、部署文档执行人工复核，确认无漏改与误改

## 5. 测试与可观测基线

- [x] 5.1 为后端关键路径（短链创建、跳转、错误处理）补充单元/集成测试
- [ ] 5.2 为前端关键交互补充测试（核心页面渲染、主要表单流程、错误提示）
- [x] 5.3 输出结构化日志字段规范（请求标识、错误类型、关键上下文）
- [x] 5.4 增加基础指标采集（创建成功率、跳转成功率、4xx/5xx 比例、延迟分位数）
- [x] 5.5 在 CI 设定发布门禁：关键测试失败即阻断发布

## 6. 发布、回滚与验收

- [x] 6.1 编写部署步骤文档（本地、预发布、生产）并明确环境差异项
- [x] 6.2 预发布环境完成一次端到端演练并记录验证结果
- [x] 6.3 制定并演练回滚流程（旧镜像/旧网关配置快速恢复）
- [x] 6.4 形成面向面试展示的结果包（测试报告、关键指标快照、架构说明）
