## 修改需求

### 需求: 在创建短链接时支持自定义域名

系统目前忽略了创建短链接请求中的域名参数。需要修改逻辑，使得系统优先使用用户提供的域名，仅在未提供时使用默认域名。

#### 场景: 使用指定的域名创建短链接
- **当** 接收到的短链接创建请求中包含非空的 `domain` 字段时
- **那么** 系统应使用该域名构造完整短链接（`fullShortUrl`）

#### 场景: 未指定域名时使用默认域名
- **当** 接收到的短链接创建请求中 `domain` 字段为空或为 null 时
- **那么** 系统应回退到配置文件中定义的默认域名来构造完整短链接
