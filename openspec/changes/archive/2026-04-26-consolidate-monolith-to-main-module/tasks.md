## 1. 模块结构收敛与工程基线

- [x] 1.1 盘点现有后端模块与依赖关系，确认仅保留 `main` 作为唯一业务与启动模块
- [x] 1.2 调整父级 Maven 聚合与子模块声明，移除 `admin`、`project`、`gateway` 等目标删除模块引用
- [x] 1.3 统一工程目录与命名规范，明确单模块下的包结构与配置入口
- [x] 1.4 执行一次全量构建验证，确保结构收敛后工程仍可编译

## 2. 业务能力迁移与代码合并

- [x] 2.1 梳理 `admin`、`project` 的保留能力清单，标注必须迁移与可下线内容
- [x] 2.2 将保留的 Service/Controller/Repository 等实现迁移至 `main` 并完成包路径调整
- [x] 2.3 清理迁移后重复实现与失效代码，确保同一能力仅保留单一实现
- [x] 2.4 校验核心短链能力（创建、查询、跳转）在 `main` 内链路完整可用

## 3. 去网关与去分布式依赖治理

- [x] 3.1 删除 `gateway` 模块源码、构建配置、启动脚本与文档引用
- [x] 3.2 移除 Nacos 相关依赖、配置项与初始化逻辑（含 profile 中的残留配置）
- [x] 3.3 移除远程调用相关依赖与适配层（Feign/RPC 等），改造为本地方法调用
- [x] 3.4 执行全仓检索，确认不存在对网关、注册中心、远程调用的残留引用

## 4. 配置、构建与部署收敛

- [x] 4.1 统一 `application.yaml` 与环境配置模板，确保独立单体可本地直接运行
- [x] 4.2 调整 Docker/Compose 编排，仅保留单体后端及必要配套服务
- [x] 4.3 更新 CI 流程为单产物构建、测试、打包与发布路径
- [x] 4.4 完成部署文档与运行手册更新，明确“无网关、无 Nacos、无远程调用”基线

## 5. 测试验证与交付门禁

- [x] 5.1 补齐并执行关键路径自动化测试（短链创建、跳转、异常处理）
- [x] 5.2 增加迁移后冒烟脚本，验证单体启动、健康检查、核心 API 可用性
- [x] 5.3 验证可观测性基线（结构化日志与核心指标）在单体部署下可采集
- [x] 5.4 形成迁移验收清单与回滚操作说明，完成最终发布前评审
