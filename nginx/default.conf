# Default server - reject requests with unrecognized Host header
# Returns 444 (close connection) for unknown domains or direct IP access

server {
    listen 80 default_server;
    server_name _;

    return 444;
}
