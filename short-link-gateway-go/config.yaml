server:
  port: 8080

nacos:
  # 4GB production profile keeps nacos disabled; the gateway statically routes
  # to `default_target`. Flip to true + bring up a nacos container if dynamic
  # discovery is required (non-4GB profile).
  enabled: false
  host: "nacos"
  port: 8848
  namespace: ""
  timeout_ms: 5000
  refresh_interval: 30

routes:
  - path_prefix: "/api/short-link/v1"
    service_name: "short-link-project"
    default_target: "http://project:8001"
    rate_limit:
      enabled: true
      qps: 1000
  - path_prefix: "/api/short-link/admin/v1"
    service_name: "short-link-admin"
    default_target: "http://admin:8002"
    rate_limit:
      enabled: true
      qps: 500

redis:
  host: "redis"
  port: 6379
  password: ""
  db: 0

cors:
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"
    - "http://linkhub.nym.asia"
    - "http://log.nym.asia"
  allowed_methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "OPTIONS"
  allowed_headers:
    - "Origin"
    - "Content-Type"
    - "Authorization"
    - "X-Trace-ID"
    - "userId"
    - "username"
    - "realName"
    - "token"
  expose_headers:
    - "X-Trace-ID"
  allow_credentials: true
  max_age: 86400

log_collector:
  endpoint: http://linkhub-log-collector:8081/api/v1/logs/ingest
  level: "info"
